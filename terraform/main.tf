# ---------------------------------------------------------
# main.tf
# Terraform プロジェクトのエントリーポイント
# ※ リソースは各ファイル（vpc.tf / subnets.tf / alb.tf など）に分割済み
# ---------------------------------------------------------

terraform {
  required_version = ">= 1.5.0"
}
